# Workspace Agent Guidelines

## Auto Git Push Rule
- Automatically stage (`git add`), commit (`git commit -m "..."`), and push (`git push`) to GitHub immediately after completing any code modifications, fixes, or feature implementations. Do not wait for the user to explicitly remind or ask for a `git push`.
